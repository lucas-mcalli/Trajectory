import { Storage } from "@plasmohq/storage"
import FlightEvent from "~/components/ui/flightEvent"
import AccomodationEvent from "~components/ui/stayEvent"
import Gap from "~components/ui/gap"
import { determineGapHeight } from "~/helpers"

export default function Timeline(militaryTime: boolean) {
  // TODO: create timeline with storage schema clearly defined (after creating components), with flight, accomodation, and day trip events.
}